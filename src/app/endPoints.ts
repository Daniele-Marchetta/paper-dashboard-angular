export const serverUrl:string = `http://localhost:8080/`;

export const registerUrl = `${serverUrl}api/auth/register`

export const loginUrl =  `${serverUrl}api/auth/authenticate`

export const logoutUrl = `${serverUrl}api/auth/logout`
